[phases.setup]
aptPkgs = [
  "...",
  "tesseract-ocr",
  "tesseract-ocr-eng",
  "libgl1",
  "libglib2.0-0",
  "libgomp1"
]

[start]
cmd = "uvicorn backend.app:app --host 0.0.0.0 --port ${PORT:-8000}"
